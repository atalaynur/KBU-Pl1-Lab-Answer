#include <stdio.h>

int main()
{
	float x,sum,fact;
	int i,n;
	printf("Input the value of x :");
	scanf("%f",&x);
	printf("Input number of terms : ");
	scanf("%d",&n);

    	for (i=n-1;i>0;i--)
	{
        fact=fact*i;
	}

	sum =1; fact = 1;

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < i j++)
        {
            sum+=x/array[j];
        }
        
    }
    

return 0;
} 